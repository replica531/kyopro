変数.set();//全てのビットを1にする
変数.set(1にする//特定のビットを1にする
変数.set(位置, 値);//特定のビットの値を変更する
変数.reset();//全てのビットを0にする
変数.reset(0にする位置);//特定のビットを0にする
変数.flip();//全てのビットを反転する
変数.flip(反転する位置);//特定のビットを反転する
変数.test(調べる位置);//特定のビットが1になっているかを調べる
変数.all()//全てのビットが1になっているかを判定する
変数.any()//いずれかのビットが1になっているかを判定する
変数.count()//1のビットの個数を数える
cout << 変数;//ビット列を出力する
変数.to_string()//ビット列を文字列化する
